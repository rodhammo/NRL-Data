# Converters (Deprecated)

> **These notebooks have been replaced by `data/converter.py`.** Use the new script instead:
>
> ```bash
> python data/converter.py --type all --years 2023
> ```

The legacy notebooks (`match_to_txt_2023.ipynb`, `player_to_txt_2023.ipynb`) are kept for reference but are no longer maintained.
